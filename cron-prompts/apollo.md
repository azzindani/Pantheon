DAILY BRIEFING — the numbers at the edge.

First: run `date '+%A %d %b %Y %H:%M %Z'` to anchor the exact time of this run. Prioritize sources and data published in the last 24-48 hours — always note the publication date or how recent each source is. Do not reuse topics already covered in your recent memory.

Deliver ONE deep, current dispatch where you do what you do best: turn a live claim, price, or dataset into a quantified answer with its uncertainty. Pick something topical and measurable, build the model, expose the drivers, run the sensitivities, and report a range — not false precision. Show your work.

Each run, pick a DIFFERENT angle you have not covered recently (consult your memory; rotate widely). Rotate across, and go beyond, this menu — be proactive and surface things I did not ask for:
- A real DCF / LBO / comps / three-statement model on a current name, with a sensitivity table on the 2-3 drivers that move it most
- Monte Carlo or scenario distribution for a live event (rate decision, earnings, deal close) — give the probabilities
- Factor exposure, volatility/correlation regime, drawdown or beta decomposition on a current portfolio or index
- A backtest of a topical strategy — state sample size, look-ahead/survivorship traps, and out-of-sample behavior
- A statistical debunk: take a number making headlines and show what it really is (base rate, denominator, artifact)
- Position sizing, Kelly, expected value, and risk of ruin on a trade people are talking about

To research, run python3 /opt/data/bin/websearch.py 'your query' (ranked web results with titles, URLs and snippets from the local SearXNG) and python3 /opt/data/bin/webfetch.py 'URL' to read a source; skip obvious spam/gibberish results and prefer credible sources. Ground it in CURRENT real prices, filings, and data — do not rely on memory alone.

Deliver, as a tight Telegram briefing (under ~3500 chars, short paragraphs/bullets):
1. The question, stated as a measurable quantity
2. The model and its key assumptions (be explicit)
3. The answer as a RANGE, with the 2-3 inputs that drive it and a sensitivity read
4. The single weakest point in the data or method — where you might be wrong
5. SOURCE LINKS (full URLs) at the end
Finish with one number you'd watch next, and the threshold that would change your answer.
